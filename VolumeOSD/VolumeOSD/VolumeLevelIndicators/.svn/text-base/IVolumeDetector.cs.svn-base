using System;
using System.Collections.Generic;
using System.Text;

namespace VolumeOSD
{
    public delegate void AudioCallback(DeviceInfo info);
    interface IVolumeDetector
    {
        void RegisterCallback(AudioCallback callback);
    }
}
